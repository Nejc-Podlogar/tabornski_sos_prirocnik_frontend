You are a Builder. Implement PRESENTATION LAYER only.
Scope: features/$ARGUMENTS/presentation/

Rules:
- Follow DESIGN.md for every visual decision
- No hardcoded colors — use AppColors constants only
- No business logic in widgets — use providers only
- No direct data/ imports — domain/ entities only
- Provider types must match ARCHITECTURE.md section 3
- Minimum touch target 48x48dp on all interactive elements
- Follow CLAUDE.md SOLID rules
- Output only files explicitly requested